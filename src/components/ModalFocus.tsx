import {useEffect} from 'react';
export default function ModalFocus(){useEffect(()=>{let current:HTMLElement|null=null;let previous:HTMLElement|null=null;const focusables=(root:HTMLElement)=>Array.from(root.querySelectorAll<HTMLElement>('button:not([disabled]),input:not([disabled]),textarea:not([disabled]),select:not([disabled]),a[href],[tabindex="0"]')).filter(e=>e.getClientRects().length>0);const inspect=()=>{const dialog=document.querySelector<HTMLElement>('[role="dialog"]');if(dialog===current)return;if(dialog){previous=document.activeElement as HTMLElement;current=dialog;if(!dialog.contains(document.activeElement))focusables(dialog)[0]?.focus();}else{current=null;previous?.focus();previous=null;}};const observer=new MutationObserver(inspect);observer.observe(document.body,{subtree:true,childList:true});const key=(e:KeyboardEvent)=>{if(!current)return;if(e.key==='Tab'){const all=focusables(current),first=all[0],last=all.at(-1);if(!first){e.preventDefault();return;}if(e.shiftKey&&(document.activeElement===first||!current.contains(document.activeElement))){e.preventDefault();last?.focus();}else if(!e.shiftKey&&(document.activeElement===last||!current.contains(document.activeElement))){e.preventDefault();first.focus();}}if(e.key==='Escape'){const close=Array.from(current.querySelectorAll<HTMLButtonElement>('button')).find(b=>b.getAttribute('aria-label')?.startsWith('Fechar')||['Cancelar','Concluir'].includes(b.textContent?.trim()??''));close?.click();}};document.addEventListener('keydown',key);return()=>{observer.disconnect();document.removeEventListener('keydown',key);};},[]);return null;}
